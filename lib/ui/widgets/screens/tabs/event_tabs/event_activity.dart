import 'package:flutter/material.dart';

class EventActivity extends StatelessWidget {
  const EventActivity({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text('Event Activity Screen')),
    );
  }
}
