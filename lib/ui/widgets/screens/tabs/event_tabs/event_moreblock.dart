import 'package:flutter/material.dart';

class EventMoreblock extends StatelessWidget {
  const EventMoreblock({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text('Event More Block Screen')),
    );
  }
}
