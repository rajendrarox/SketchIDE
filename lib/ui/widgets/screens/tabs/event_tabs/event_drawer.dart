import 'package:flutter/material.dart';

class EventDrawer extends StatelessWidget {
  const EventDrawer({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text('Event Drawer Screen')),
    );
  }
}
