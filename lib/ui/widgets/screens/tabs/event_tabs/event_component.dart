import 'package:flutter/material.dart';

class EventComponent extends StatelessWidget {
  const EventComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text('Event Component Screen')),
    );
  }
}
